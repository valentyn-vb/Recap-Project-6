POSTGRES_DB=learning_companion
POSTGRES_USER=learning_companion
POSTGRES_PASSWORD=learning_companion
POSTGRES_PORT=5433

# Required by the backend app (bootRun) — generate via: openssl rand -base64 48
JWT_SECRET=
# Optional, defaults to 1 hour
JWT_EXPIRATION_MS=3600000
