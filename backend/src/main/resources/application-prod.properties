# Production schema management: Flyway owns the schema, Hibernate only validates
# that the entity mappings match the migrated tables (never mutates them).
spring.flyway.enabled=true
spring.jpa.hibernate.ddl-auto=validate
